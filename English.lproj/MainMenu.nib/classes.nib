{
    IBClasses = (
        {
            CLASS = Controller; 
            LANGUAGE = ObjC; 
            OUTLETS = {appMenu = NSMenu; prefs = NSPanel; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}